#include <stdio.h>
void main()
{
    int numOne, numTwo;
    printf("\nEnter first number:");
    scanf("%d", &numOne);
    printf("\nEnter second number:");
    scanf("%d", &numTwo);
    printf("\nSum = %d", (numOne + numTwo));
}